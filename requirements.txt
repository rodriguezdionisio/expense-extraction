# Expense Extraction & Processing System - Dependencies
# Optimized requirements for the simplified architecture

# Core data processing
pandas>=2.0.0                    # DataFrame processing and CSV generation
requests>=2.31.0                 # HTTP requests to Fudo API
python-dotenv>=1.0.0             # Environment variables management

# Google Cloud integration
google-cloud-secret-manager>=2.16.0  # Secure API credentials storage
google-auth>=2.23.0              # GCP authentication

# Timezone and date handling
pytz>=2023.3                     # Timezone conversions (America/Bogota)

# Note: Standard library modules used (no additional requirements):
# - json (JSON processing)
# - os (file system operations) 
# - sys (command line arguments)
# - logging (application logging)
# - datetime (date/time handling)
# - pathlib (path operations)
